
<% @users.each do |user| %>
  <%= link_to user.name, user_path(user) %> <br>


  <% user.trips.each do |trip| %>

  <%= link_to trip.country.name, country_path(trip.country) %><br>
  <%= trip.comment %><br>

  <% end %>
<% end %>


<%= link_to "Log Out", logout_path %>
