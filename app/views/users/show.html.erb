<%= @user.name %><br>

<% @user.trips.each do |trip| %>
  Country:<%= trip.country.name %><br>
  Comment:<%= trip.comment %><br>
  Recommend?<%= trip.recommend? %><br>
  <% end %>


<% if current_user.id == @user.id %>
<%= link_to "Log Out", logout_path %>
<% end %> 
