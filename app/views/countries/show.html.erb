

<%= @country.name %><br>

<% @country.trips.each do |trip| %>
  <%= link_to trip.user.name, user_path(trip.user) %><br>
  <%= trip.comment %>  <br>
  <%= recommended? trip %> <br>
  <% end %>

  <br>
  <br>

  <%= link_to "Back to all countries", countries_path %><br>
  <%= link_to "Back to all trips", users_path %><br>
